<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if (successMsg.length > 0) { %>
        <p style="color: green;"><%= successMsg[0] %></p>
    <% } %>

    <% if (errMsg.length > 0) { %>
        <p style="color: red;"><%= errMsg[0] %></p>
    <% } %>

    <h1>hello, <%= name %></h1>
</body>
</html>
